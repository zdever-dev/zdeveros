{
  "name": "zdever-os",
  "version": "1.0.0",
  "description": "ZdeVer OS — Repair Shop Management System",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BE,FE\" -c \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "setup": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../backend && npm run migrate",
    "migrate": "cd backend && npm run migrate"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
